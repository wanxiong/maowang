<view wx:if="{{animateFlg}}">
  <view  class='popup-box'>
    <view class="ps  {{show ? 'animated ' + style_in : 'animated ' + style_out}}" catchtap="{{trigger ? '' : 'triggerClose'}}" animationend="popAniendState">
      <slot></slot>
    </view>
  </view>
</view>


